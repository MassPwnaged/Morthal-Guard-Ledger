{
  "name": "morthal-guard-ledger",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "users": "node scripts/make-users.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}